name: StackExchange Playground
on:
  workflow_dispatch:
    inputs:
      smartcontracts:
        description: 'Include the Smart Contracts team.'
        type: boolean
      devhub:
        description: 'Include the DevHub team.'
        type: boolean
      subxt:
        description: 'Include the subxt team.'
        type: boolean
      support:
        description: 'Include the Support team.'
        type: boolean
      integrations:
        description: 'Include the Integrations team.'
        type: boolean
  schedule:
    - cron: '*/5 * * * *'

jobs:

  send_if:
    if: ${{ github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello World

  send_always:
    if:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event_name == 'schedule' || inputs.smartcontracts }}
      - run: echo ${{ github.event_name == 'schedule' }}
      - run: echo ${{ github.event_name }}

  send_smartcontracts_digest:
    if: ${{ github.event_name == 'schedule' || inputs.smartcontracts }}
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello World

  send_devhub_digest:
    if: ${{ github.event_name == 'schedule' || inputs.devhub }}
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello World

  send_subxt_digest:
    if: ${{ github.event_name == 'schedule' || inputs.subxt }}
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello World

  send_support_digest:
    if: ${{ github.event_name == 'schedule' || inputs.support }}
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello World

  send_integrations_digest:
    if: ${{ github.event_name == 'schedule' || inputs.integrations }}
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello World